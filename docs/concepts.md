# React Web – Learning Curve

Basics HTML7/DOM, CSS

Master JavaScript, I recommend this Course:  
# https://www.freecodecamp.org/learn/javascript-v9/

Event Handling, e.g onClick property on button element in JS -> Transition to React is easier

Concepts of Fetching with Ajax/Fetch API/Axios: can be used the same way in React

# TypeScript Basics – Intermediate: https://www.typescriptlang.org/docs/handbook/typescript-in-5-minutes.html

#  Important: interface vs type, composite types, partial, omit, pick, type inference, keyof/typeof

# If the above is fine, now to React…

Learn JSX Syntax to grasp the Feeling, e.g rendering simple HTML + JS, or using JS map with HTML to
render some HTML -> Then transfer to TSX, which is basically JSX but with TS instead of JS

# Core Important Concepts:

* Managing State is achieved through a broad setting:

.) Hooks: useState, useReducer, useMemo, useCallback useEffect, useParam
.) Passing Props, Lifting state up, Context/Provider -> Avoids Props Drilling 
.) Concept of Render, Conditional Rendering

# Concept of Reusability: 
Design your App in a way that individual Components can break without
affecting each other. 
Other useful knowledge: 
- Conditional Rendering
- Mastering Props and State is CRUCIAL

# Extracting Logic to reusable Custom Hooks or Individual Components, util files is just as important

Then you can transfer easily to State Management Libraries like:
- Zustand/Redux

Routing: Basic setup of Browser router and navigation using hooks
# https://reactrouter.com/upgrading/v6

How to pass route parameters: 
# https://reactrouter.com/api/hooks/useParams

Basics of: 
- package.json -> how to write scripts for commands
- tsconfig.json
- lock files e.g. yarn.lock/package.lock.json

npm install, uninstall, audit, --force, list, etc.
for typescript projects: type definitions if not provided





